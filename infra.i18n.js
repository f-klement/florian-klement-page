// Strings for infra.html. Merged into the main dictionary by main.js.
window.I18N_EXTRA = {
  en: {
    "hl.back":            "Back to overview",
    "hl.title":           "Private infrastructure",
    "hl.subtitle":        "A self-hosted Kubernetes platform, reproducible from code",
    "hl.intro":           "Four nodes running K3s, backed by a TrueNAS SCALE storage host and fronted by a UniFi edge. The whole platform is rebuilt from a bootstrap script and one Helm chart; the only manual step is the one-time initialisation of the secrets store. The numbers below are a snapshot from September 2026.",

    "hl.stat.nodes":      "nodes",
    "hl.stat.nodes.sub":  "18 cores, 62 GiB memory",
    "hl.stat.pods":       "pods",
    "hl.stat.pods.sub":   "144 containers, 25 namespaces",
    "hl.stat.argo":       "Argo CD applications",
    "hl.stat.argo.sub":   "all auto-synced from main",
    "hl.stat.deploy":     "Deployments",
    "hl.stat.deploy.sub": "plus 39 CronJobs, 8 DaemonSets",
    "hl.stat.netpol":     "NetworkPolicies",
    "hl.stat.netpol.sub": "default deny where it matters",
    "hl.stat.images":     "container images",
    "hl.stat.images.sub": "every one pinned by digest",

    "hl.section.arch":    "Architecture",
    "hl.arch.caption":    "The recursive edge is the thing to hold on to: the storage host also runs one of the compute nodes, as a privileged LXC sharing the host kernel. That single fact explains the storage placement rules, the loopback NFS deadlock described below, and why a NAS reboot is a cluster event.",

    "dg.edge":            "Edge",
    "dg.internet":        "Internet",
    "dg.lan":             "LAN clients",
    "dg.cf":              "Cloudflare proxy",
    "dg.cf.sub":          "WAF and rate limiting, opt-in per host",
    "dg.router":          "UniFi edge router",
    "dg.router.sub":      "firewall, NAT, WireGuard",
    "dg.dnsonly":         "DNS-only hosts",
    "dg.compute":         "Compute: K3s, 4 nodes",
    "dg.traefik":         "Traefik ingress",
    "dg.traefik.sub":     "single entry point, floating VIP",
    "dg.mw":              "CrowdSec + Authelia",
    "dg.mw.sub":          "WAF, bans, forwardAuth",
    "dg.apps":            "Application workloads",
    "dg.apps.sub":        "one Helm chart, 16 services",
    "dg.plat":            "Platform components",
    "dg.plat.sub":        "19 Helm releases",
    "dg.storage":         "Storage: TrueNAS SCALE, bare metal",
    "dg.zfs":             "ZFS pools",
    "dg.zfs.sub":         "NVMe tiers for configs and cluster volumes, HDD pool for bulk data",
    "dg.lxc":             "also hosts one K3s node as an LXC",

    "hl.section.layers":  "How it is built",
    "hl.details":         "How it works",

    "hl.delivery.title":  "Delivery",
    "hl.delivery.lead":   "A merge to main is the deploy. Renovate proposes, CI gates, Argo CD applies, and no single credential can both author a change and ship it.",
    "hl.delivery.p1":     "GitLab CI runs tests, dependency audits and a Trivy scan for secrets, dependencies and the image before anything reaches the registry. Renovate opens version bump merge requests and automerges routine ones; roughly a dozen high blast radius packages and every major version wait for a human click.",
    "hl.delivery.p2":     "All 32 Argo CD applications auto-sync, so there is exactly one gate per change. Images are pinned by digest, each written in exactly one place. A gate that always passes is not a gate: where a second manual step was removed, it was replaced by a CI check that fails the merge, not by trust.",
    "hl.delivery.p3":     "Three things sit deliberately outside GitOps: node upgrades (they cordon nodes and swap binaries, not containers), the vendored Wazuh manifests, and secret values, which no reconciler can supply. Argo CD replaced a hand written redeploy poller that classified commits by changed path and misidentified more than half of the chart bumps.",

    "hl.security.title":  "Security",
    "hl.security.lead":   "Five tools, five boundaries, one pane. Each covers something the others cannot see, and all of them forward into Wazuh.",
    "hl.security.p1":     "The edge router handles packets and intrusion prevention. CrowdSec reads the ingress logs and runs an application layer WAF, banning at the edge with escalating durations; a firewall bouncer on every node drops banned sources on all ports, and its chains fail open, so a crash costs protection, never access. Falco watches syscalls through eBPF, ClamAV scans on a schedule, Lynis audits host posture weekly.",
    "hl.security.p2":     "Wazuh (HIDS and SIEM) correlates all of it, with curated realtime file integrity monitoring on every node. The two hosts that cannot run an agent, the NAS and the router, report over syslog through a relay so both Loki and Wazuh receive a copy. High severity alerts page via Telegram.",
    "hl.security.p3":     "Argo CD is the most privileged workload and is fenced accordingly: a default deny NetworkPolicy pair limits it to DNS, the API server and outbound HTTPS. The chart's own policies had to be disabled to get there, because its shipped server policy allowed ingress from every pod in the cluster, and NetworkPolicies are additive. Found by reading the live object, not the chart.",

    "hl.observability.title": "Observability",
    "hl.observability.lead":  "Grafana Alloy collects logs, metrics and traces into Loki, Mimir and Tempo on MinIO. Alerting is split between the SIEM and Gatus, and the pager has a check that watches the pager.",
    "hl.observability.p1":    "All three signals persist to MinIO on NVMe. A nightly job mirrors the day's objects to a compressed dataset on the HDD pool, so the spinning disks stay asleep instead of absorbing continuous writes.",
    "hl.observability.p2":    "Gatus does not probe ports. Its interesting checks query Mimir: unavailable replicas anywhere in the app namespace, exporters that have stopped producing metrics, and Argo CD applications that can no longer be rendered. That last case is invisible from every other angle, because the pods keep running while the cluster silently stops tracking git.",
    "hl.observability.p3":    "Readiness probe coverage went from 7 to 19 of 31 containers, which is what gave the replica metric meaning: a container without a probe is Ready the moment it starts. Exporter sidecars deliberately have none, because a pod is only Ready when every container is, and a probe on a sidecar would pull the application out of its Service. Monitoring failure would cause an outage.",

    "hl.identity.title":  "Identity",
    "hl.identity.lead":   "One account per person in LLDAP, one group as the single access gate. Removing someone there revokes everything at once.",
    "hl.identity.p1":     "Authelia provides the login portal and acts as OIDC provider, binding read-only to LDAP. Browser-only apps sit behind forwardAuth. Apps with native clients that cannot follow a login redirect bind to LDAP directly. Argo CD uses both: forwardAuth keeps anonymous traffic off it entirely, and OIDC tells its own RBAC who you are.",
    "hl.identity.p2":     "LDAP rather than an OIDC-first design because it is the common denominator across the services that matter here. Apps behind forwardAuth are ClusterIP only: a NodePort would SNAT traffic into the pod CIDR that the identity header trusts, making it forgeable from any host on the LAN.",

    "hl.storage.title":   "Storage",
    "hl.storage.lead":    "ZFS on the NAS, three pools tuned by record size, three paths into pods. Databases stay off NFS by design.",
    "hl.storage.p1":      "An NVMe pool with small record sizes holds service configs and databases, bind-mounted as hostPath into the LXC node. A second NVMe pool backs democratic-csi for dynamically provisioned volumes (observability, Wazuh, Endpaper). The HDD pool serves bulk data over static NFS volumes and holds the cold archive and snapshot replication.",
    "hl.storage.p2":      "SQLite and MariaDB locking over NFS is unsafe, so anything database-bearing uses hostPath. iSCSI is impossible here: the LXC worker's network namespace blocks the kernel initiator, which is also why Longhorn was abandoned. Backups are ZFS snapshots replicated to a second pool, on-box only.",

    "hl.section.lessons": "Lessons from incidents",
    "hl.lesson1.title":   "The loopback NFS deadlock",
    "hl.lesson1.body":    "The LXC's NFS client and the host's NFS server share one kernel, one page cache and one reclaim path. Under a burst of concurrent writers an nfsd thread entered direct reclaim, landed on dirty pages of an NFS client mount, and waited on an RPC to a server whose every thread was in the same wait. Every export was down for hours and needed a power cut. The media path now bypasses the NFS client entirely via hostPath, which is a reduction, not a cure, and documented as such.",
    "hl.lesson2.title":   "Ready for 39 hours through a storage outage",
    "hl.lesson2.body":    "A pod stayed healthy through a total storage outage because its probe ran a real query through an already open SQLite file descriptor served from page cache, while fresh opens on the same mount hung forever. A probe that reuses a long-lived handle measures the process, not its storage. Probes now perform a namespace operation with an internal timeout shorter than the probe timeout.",
    "hl.lesson3.title":   "The deploy console reachable from any pod",
    "hl.lesson3.body":    "Argo CD's chart ships a NetworkPolicy for its server that allows ingress from any pod in any namespace. Policies are additive, so a narrower one cannot subtract it, and the console bypassed both the LAN-only allowlist and the login for anything inside the cluster. Found by reading the live object after install; fixed by disabling the chart's policies and writing a default deny pair.",
    "hl.lesson4.title":   "CI starving etcd",
    "hl.lesson4.body":    "Build pods once saturated the control plane and restarted k3s 26 times in an afternoon. CI is now pinned hard to a dedicated node, not merely preferred, because a scheduling preference would let it recur under memory pressure, the exact condition that caused it.",
    "hl.lesson5.title":   "The alert that fired once",
    "hl.lesson5.body":    "Gatus never re-notifies while an alert stays triggered, so an eight day outage produced exactly one message, on day one. Wazuh rules now provide the escalation Gatus structurally cannot, and every scheduled job whose only output is an exit status has something that reads that status. A suppression rule is replayed against real events rather than verified by reading, because silence cannot distinguish a working filter from a source that went quiet.",

    "hl.section.limits":  "Known constraints",
    "hl.limit1":          "The LXC node runs privileged and unconfined, which the nested kubelet needs. A pod escape could reach host root and the bind-mounted datasets. An unprivileged migration with ID mapping is planned and clone-tested.",
    "hl.limit2":          "Backups are on-box only. Snapshots replicate to a second pool on the same host; there is no offsite copy yet.",
    "hl.limit3":          "A NAS reboot is a cluster event. It takes the LXC node down and, because the CI node hangs off the same bridge, CI with it.",
  },
  de: {
    "hl.back":            "Zurück zur Übersicht",
    "hl.title":           "Private Infrastruktur",
    "hl.subtitle":        "Eine selbst gehostete Kubernetes-Plattform, aus Code reproduzierbar",
    "hl.intro":           "Vier Nodes mit K3s, dahinter ein TrueNAS-SCALE-Storage-Host, davor ein UniFi-Edge. Die gesamte Plattform wird aus einem Bootstrap-Skript und einem Helm-Chart neu aufgebaut; der einzige manuelle Schritt ist die einmalige Initialisierung des Secrets-Speichers. Die Zahlen unten sind ein Snapshot vom September 2026.",

    "hl.stat.nodes":      "Nodes",
    "hl.stat.nodes.sub":  "18 Kerne, 62 GiB Arbeitsspeicher",
    "hl.stat.pods":       "Pods",
    "hl.stat.pods.sub":   "144 Container, 25 Namespaces",
    "hl.stat.argo":       "Argo-CD-Applications",
    "hl.stat.argo.sub":   "alle automatisch aus main synchronisiert",
    "hl.stat.deploy":     "Deployments",
    "hl.stat.deploy.sub": "dazu 39 CronJobs, 8 DaemonSets",
    "hl.stat.netpol":     "NetworkPolicies",
    "hl.stat.netpol.sub": "Default Deny, wo es darauf ankommt",
    "hl.stat.images":     "Container-Images",
    "hl.stat.images.sub": "jedes per Digest gepinnt",

    "hl.section.arch":    "Architektur",
    "hl.arch.caption":    "Die rekursive Kante ist das Entscheidende: Der Storage-Host betreibt zugleich einen der Compute-Nodes, als privilegierter LXC mit dem Kernel des Hosts. Diese eine Tatsache erklärt die Platzierungsregeln für Storage, den unten beschriebenen Loopback-NFS-Deadlock und warum ein NAS-Neustart ein Cluster-Ereignis ist.",

    "dg.edge":            "Edge",
    "dg.internet":        "Internet",
    "dg.lan":             "LAN-Clients",
    "dg.cf":              "Cloudflare-Proxy",
    "dg.cf.sub":          "WAF und Rate Limiting, pro Host opt-in",
    "dg.router":          "UniFi-Edge-Router",
    "dg.router.sub":      "Firewall, NAT, WireGuard",
    "dg.dnsonly":         "Hosts nur per DNS",
    "dg.compute":         "Compute: K3s, 4 Nodes",
    "dg.traefik":         "Traefik-Ingress",
    "dg.traefik.sub":     "einziger Eingang, wandernde VIP",
    "dg.mw":              "CrowdSec + Authelia",
    "dg.mw.sub":          "WAF, Sperren, forwardAuth",
    "dg.apps":            "Anwendungs-Workloads",
    "dg.apps.sub":        "ein Helm-Chart, 16 Dienste",
    "dg.plat":            "Plattform-Komponenten",
    "dg.plat.sub":        "19 Helm-Releases",
    "dg.storage":         "Storage: TrueNAS SCALE, Bare Metal",
    "dg.zfs":             "ZFS-Pools",
    "dg.zfs.sub":         "NVMe-Stufen für Configs und Cluster-Volumes, HDD-Pool für Massendaten",
    "dg.lxc":             "hostet zugleich einen K3s-Node als LXC",

    "hl.section.layers":  "Aufbau",
    "hl.details":         "Wie es funktioniert",

    "hl.delivery.title":  "Delivery",
    "hl.delivery.lead":   "Ein Merge nach main ist das Deployment. Renovate schlägt vor, CI prüft, Argo CD wendet an, und kein einzelnes Credential kann eine Änderung sowohl schreiben als auch ausrollen.",
    "hl.delivery.p1":     "GitLab CI führt Tests, Abhängigkeitsprüfungen und einen Trivy-Scan auf Secrets, Abhängigkeiten und das Image aus, bevor etwas die Registry erreicht. Renovate öffnet Merge Requests für Versionsanhebungen und merged Routinefälle automatisch; rund ein Dutzend Pakete mit großem Wirkungsradius und jede Major-Version warten auf einen menschlichen Klick.",
    "hl.delivery.p2":     "Alle 32 Argo-CD-Applications synchronisieren automatisch, es gibt also genau ein Gate pro Änderung. Images sind per Digest gepinnt, jeder Digest steht an genau einer Stelle. Ein Gate, das immer durchlässt, ist kein Gate: Wo ein zweiter manueller Schritt entfernt wurde, ersetzt ihn ein CI-Check, der den Merge scheitern lässt, nicht Vertrauen.",
    "hl.delivery.p3":     "Drei Dinge liegen bewusst außerhalb von GitOps: Node-Upgrades (sie sperren Nodes und tauschen Binaries, keine Container), die eingebetteten Wazuh-Manifeste und die Werte der Secrets, die kein Reconciler liefern kann. Argo CD ersetzte einen selbst geschriebenen Redeploy-Poller, der Commits nach geänderten Pfaden klassifizierte und mehr als die Hälfte der Chart-Anhebungen falsch einordnete.",

    "hl.security.title":  "Sicherheit",
    "hl.security.lead":   "Fünf Werkzeuge, fünf Grenzen, eine Oberfläche. Jedes deckt ab, was die anderen nicht sehen können, und alle leiten an Wazuh weiter.",
    "hl.security.p1":     "Der Edge-Router übernimmt Pakete und Intrusion Prevention. CrowdSec liest die Ingress-Logs und betreibt eine WAF auf Anwendungsebene, sperrt am Edge mit eskalierenden Dauern; ein Firewall-Bouncer auf jedem Node verwirft gesperrte Quellen auf allen Ports, und seine Chains fallen offen aus, ein Absturz kostet also Schutz, nie Zugang. Falco beobachtet Syscalls über eBPF, ClamAV scannt nach Zeitplan, Lynis prüft wöchentlich die Host-Härtung.",
    "hl.security.p2":     "Wazuh (HIDS und SIEM) korreliert alles, mit kuratierter Echtzeit-Dateiintegritätsüberwachung auf jedem Node. Die beiden Hosts, die keinen Agenten ausführen können, das NAS und der Router, melden per Syslog über ein Relay, sodass Loki und Wazuh je eine Kopie erhalten. Alarme hoher Stufe gehen per Telegram raus.",
    "hl.security.p3":     "Argo CD ist der privilegierteste Workload und entsprechend eingezäunt: Ein Default-Deny-NetworkPolicy-Paar beschränkt es auf DNS, den API-Server und ausgehendes HTTPS. Die eigenen Policies des Charts mussten dafür abgeschaltet werden, weil die mitgelieferte Server-Policy Ingress von jedem Pod im Cluster erlaubte, und NetworkPolicies sind additiv. Gefunden durch Lesen des Live-Objekts, nicht des Charts.",

    "hl.observability.title": "Observability",
    "hl.observability.lead":  "Grafana Alloy sammelt Logs, Metriken und Traces in Loki, Mimir und Tempo auf MinIO. Alarmierung ist zwischen SIEM und Gatus aufgeteilt, und der Pager hat einen Check, der den Pager überwacht.",
    "hl.observability.p1":    "Alle drei Signale landen auf MinIO auf NVMe. Ein nächtlicher Job spiegelt die Objekte des Tages in ein komprimiertes Dataset auf dem HDD-Pool, damit die Festplatten schlafen, statt dauerhaft Schreibzugriffe aufzunehmen.",
    "hl.observability.p2":    "Gatus prüft keine Ports. Die interessanten Checks fragen Mimir ab: nicht verfügbare Replicas irgendwo im App-Namespace, Exporter, die keine Metriken mehr liefern, und Argo-CD-Applications, die nicht mehr gerendert werden können. Letzteres ist aus jedem anderen Blickwinkel unsichtbar, weil die Pods weiterlaufen, während der Cluster still aufhört, git zu folgen.",
    "hl.observability.p3":    "Die Abdeckung mit Readiness-Probes stieg von 7 auf 19 von 31 Containern, und erst das gab der Replica-Metrik Bedeutung: Ein Container ohne Probe ist Ready, sobald er startet. Exporter-Sidecars haben bewusst keine, denn ein Pod ist nur Ready, wenn jeder Container es ist, und eine Probe am Sidecar würde die Anwendung aus ihrem Service nehmen. Ein Ausfall des Monitorings würde einen Ausfall verursachen.",

    "hl.identity.title":  "Identität",
    "hl.identity.lead":   "Ein Konto pro Person in LLDAP, eine Gruppe als einziges Zugangs-Gate. Wer dort entfernt wird, verliert alles auf einmal.",
    "hl.identity.p1":     "Authelia stellt das Login-Portal und agiert als OIDC-Provider, mit lesendem Bind an LDAP. Reine Browser-Apps stehen hinter forwardAuth. Apps mit nativen Clients, die keiner Login-Weiterleitung folgen können, binden direkt an LDAP. Argo CD nutzt beides: forwardAuth hält anonymen Verkehr komplett fern, und OIDC sagt seinem RBAC, wer man ist.",
    "hl.identity.p2":     "LDAP statt eines OIDC-first-Designs, weil es der gemeinsame Nenner der Dienste ist, auf die es hier ankommt. Apps hinter forwardAuth sind ausschließlich ClusterIP: Ein NodePort würde Verkehr per SNAT in den Pod-CIDR umschreiben, dem der Identitäts-Header vertraut, und ihn damit von jedem Host im LAN fälschbar machen.",

    "hl.storage.title":   "Storage",
    "hl.storage.lead":    "ZFS auf dem NAS, drei Pools nach Record Size abgestimmt, drei Wege in die Pods. Datenbanken bleiben bewusst weg von NFS.",
    "hl.storage.p1":      "Ein NVMe-Pool mit kleinen Record Sizes hält Dienst-Configs und Datenbanken, per hostPath in den LXC-Node gemountet. Ein zweiter NVMe-Pool trägt democratic-csi für dynamisch bereitgestellte Volumes (Observability, Wazuh, Endpaper). Der HDD-Pool liefert Massendaten über statische NFS-Volumes und hält das Kaltarchiv und die Snapshot-Replikation.",
    "hl.storage.p2":      "SQLite- und MariaDB-Locking über NFS ist unsicher, daher nutzt alles mit Datenbank hostPath. iSCSI ist hier unmöglich: Der Netzwerk-Namespace des LXC-Workers blockiert den Kernel-Initiator, weshalb auch Longhorn aufgegeben wurde. Backups sind ZFS-Snapshots, repliziert auf einen zweiten Pool, nur auf dem Gerät selbst.",

    "hl.section.lessons": "Gelernt aus Vorfällen",
    "hl.lesson1.title":   "Der Loopback-NFS-Deadlock",
    "hl.lesson1.body":    "NFS-Client des LXC und NFS-Server des Hosts teilen sich einen Kernel, einen Page Cache und einen Reclaim-Pfad. Bei einem Schub gleichzeitiger Schreiber geriet ein nfsd-Thread in Direct Reclaim, traf auf schmutzige Seiten eines NFS-Client-Mounts und wartete auf einen RPC an einen Server, dessen sämtliche Threads im selben Wartezustand hingen. Alle Exports waren stundenlang weg, und es brauchte einen Stromschnitt. Der Medienpfad umgeht den NFS-Client jetzt vollständig per hostPath, was eine Verringerung ist, keine Heilung, und so dokumentiert.",
    "hl.lesson2.title":   "39 Stunden Ready während eines Storage-Ausfalls",
    "hl.lesson2.body":    "Ein Pod blieb während eines kompletten Storage-Ausfalls gesund, weil seine Probe eine echte Abfrage über einen bereits offenen SQLite-Dateideskriptor aus dem Page Cache ausführte, während neue Opens auf demselben Mount ewig hingen. Eine Probe, die ein langlebiges Handle wiederverwendet, misst den Prozess, nicht seinen Storage. Probes führen jetzt eine Namespace-Operation aus, mit internem Timeout unterhalb des Probe-Timeouts.",
    "hl.lesson3.title":   "Die Deploy-Konsole, erreichbar aus jedem Pod",
    "hl.lesson3.body":    "Das Argo-CD-Chart liefert für seinen Server eine NetworkPolicy mit, die Ingress von jedem Pod in jedem Namespace erlaubt. Policies sind additiv, eine engere kann sie also nicht abziehen, und die Konsole umging für alles im Cluster sowohl die LAN-Allowlist als auch das Login. Gefunden durch Lesen des Live-Objekts nach der Installation; behoben durch Abschalten der Chart-Policies und ein eigenes Default-Deny-Paar.",
    "hl.lesson4.title":   "CI hungert etcd aus",
    "hl.lesson4.body":    "Build-Pods sättigten einmal die Control Plane und starteten k3s an einem Nachmittag 26 Mal neu. CI ist jetzt hart an einen eigenen Node gebunden, nicht nur bevorzugt, weil eine Scheduling-Präferenz das unter Speicherdruck wieder zulassen würde, genau der Zustand, der es verursacht hat.",
    "hl.lesson5.title":   "Der Alarm, der einmal kam",
    "hl.lesson5.body":    "Gatus benachrichtigt nie erneut, solange ein Alarm ausgelöst bleibt, also erzeugte ein achttägiger Ausfall genau eine Nachricht, am ersten Tag. Wazuh-Regeln liefern jetzt die Eskalation, die Gatus strukturell nicht kann, und jeder geplante Job, dessen einzige Ausgabe ein Exit-Status ist, hat etwas, das diesen Status liest. Eine Unterdrückungsregel wird gegen echte Ereignisse abgespielt statt durch Lesen geprüft, weil Stille einen funktionierenden Filter nicht von einer verstummten Quelle unterscheiden kann.",

    "hl.section.limits":  "Bekannte Einschränkungen",
    "hl.limit1":          "Der LXC-Node läuft privilegiert und ohne Confinement, was das verschachtelte kubelet braucht. Ein Pod-Ausbruch könnte Host-Root und die eingebundenen Datasets erreichen. Eine Migration auf unprivilegiert mit ID-Mapping ist geplant und am Klon getestet.",
    "hl.limit2":          "Backups gibt es nur auf dem Gerät selbst. Snapshots replizieren auf einen zweiten Pool desselben Hosts; eine Offsite-Kopie fehlt noch.",
    "hl.limit3":          "Ein NAS-Neustart ist ein Cluster-Ereignis. Er nimmt den LXC-Node mit und, weil der CI-Node an derselben Bridge hängt, auch die CI.",
  }
};
